<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
<meta name="theme-color" content="#0a0a0b" />
<title>Brandon's Send-Off — Command Center</title>
<style>
  :root {
    --bg-0: #07070a;
    --bg-1: #0f0f14;
    --bg-2: #16161e;
    --bg-3: #1d1d27;
    --line: rgba(255,255,255,0.06);
    --line-2: rgba(255,255,255,0.10);
    --text: #f5f5f7;
    --text-dim: #a1a1aa;
    --text-faint: #6b6b76;
    --accent: #f5b342;
    --accent-2: #ff8a3d;
    --accent-glow: rgba(245, 179, 66, 0.35);
    --good: #3ee08a;
    --bad: #ef4444;
    --casino: #b87cff;
    --hike: #3ee08a;
    --cabin: #f5b342;
    --bar: #ff5d8f;
  }
  * { box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
  html, body {
    margin: 0;
    padding: 0;
    background: var(--bg-0);
    color: var(--text);
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Display", "Inter", system-ui, sans-serif;
    font-feature-settings: "ss01", "cv11";
    -webkit-font-smoothing: antialiased;
    min-height: 100vh;
    background:
      radial-gradient(circle at 20% 0%, rgba(245,179,66,0.08), transparent 50%),
      radial-gradient(circle at 90% 100%, rgba(184,124,255,0.06), transparent 45%),
      var(--bg-0);
  }
  .app {
    max-width: 480px;
    margin: 0 auto;
    padding: 24px 18px 80px;
  }
  /* HEADER */
  .header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 22px;
  }
  .brand {
    display: flex;
    align-items: center;
    gap: 10px;
  }
  .brand-mark {
    width: 36px;
    height: 36px;
    border-radius: 10px;
    background: linear-gradient(135deg, var(--accent), var(--accent-2));
    display: grid;
    place-items: center;
    box-shadow: 0 8px 22px var(--accent-glow);
  }
  .brand-mark svg { width: 20px; height: 20px; color: #1a1207; }
  .brand-name {
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--text);
  }
  .brand-sub {
    font-size: 11px;
    color: var(--text-faint);
    letter-spacing: 0.05em;
    margin-top: 2px;
  }
  .live-dot {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 11px;
    color: var(--text-dim);
    background: var(--bg-2);
    border: 1px solid var(--line);
    padding: 6px 10px;
    border-radius: 999px;
  }
  .live-dot::before {
    content: '';
    width: 6px; height: 6px;
    background: var(--good);
    border-radius: 50%;
    box-shadow: 0 0 0 4px rgba(62,224,138,0.15);
    animation: pulse 1.8s infinite;
  }
  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  /* HERO */
  .hero-card {
    position: relative;
    background:
      radial-gradient(circle at 100% 0%, rgba(245,179,66,0.18), transparent 60%),
      linear-gradient(180deg, var(--bg-2), var(--bg-1));
    border: 1px solid var(--line);
    border-radius: 22px;
    padding: 22px 20px;
    overflow: hidden;
  }
  .hero-card::before {
    content: '';
    position: absolute;
    top: 0; right: 0;
    width: 160px; height: 160px;
    background: radial-gradient(circle, rgba(245,179,66,0.25), transparent 60%);
    filter: blur(20px);
    pointer-events: none;
  }
  .hero-tag {
    display: inline-block;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--accent);
    background: rgba(245,179,66,0.10);
    border: 1px solid rgba(245,179,66,0.20);
    padding: 5px 10px;
    border-radius: 6px;
  }
  .hero-title {
    font-size: 26px;
    font-weight: 800;
    letter-spacing: -0.02em;
    line-height: 1.15;
    margin: 14px 0 4px;
  }
  .hero-sub {
    color: var(--text-dim);
    font-size: 13px;
    margin-bottom: 18px;
  }
  .countdown {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 8px;
    margin-top: 16px;
  }
  .cd-cell {
    background: rgba(255,255,255,0.03);
    border: 1px solid var(--line);
    border-radius: 12px;
    padding: 12px 6px;
    text-align: center;
  }
  .cd-num {
    font-size: 24px;
    font-weight: 800;
    letter-spacing: -0.03em;
    font-variant-numeric: tabular-nums;
    color: var(--text);
    line-height: 1;
  }
  .cd-lbl {
    font-size: 10px;
    color: var(--text-faint);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-top: 6px;
  }
  .countdown-label {
    display: flex;
    align-items: center;
    gap: 8px;
    color: var(--text-faint);
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    font-weight: 600;
  }
  .countdown-label svg { width: 12px; height: 12px; color: var(--accent); }

  /* VOTING */
  .vote-banner {
    background: linear-gradient(135deg, rgba(245,179,66,0.10), rgba(255,138,61,0.05));
    border: 1px solid rgba(245,179,66,0.25);
    border-radius: 12px;
    padding: 10px 14px;
    margin-bottom: 10px;
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 12px;
    color: var(--text);
  }
  .vote-banner svg { width: 16px; height: 16px; color: var(--accent); flex-shrink: 0; }
  .vote-banner b { color: var(--accent); font-weight: 700; }
  .vote-banner.setup {
    background: linear-gradient(135deg, rgba(239,68,68,0.10), rgba(239,68,68,0.04));
    border-color: rgba(239,68,68,0.30);
  }
  .vote-banner.setup svg { color: var(--bad); }
  .vote-banner.setup b { color: var(--bad); }
  .vote-identity {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    background: var(--bg-2);
    border: 1px solid var(--line);
    border-radius: 10px;
    padding: 8px 12px;
    margin-bottom: 12px;
    font-size: 12px;
  }
  .vote-identity-name {
    color: var(--text);
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 6px;
  }
  .vote-identity-name span { color: var(--text-faint); font-weight: 500; }
  .vote-identity-name svg { width: 12px; height: 12px; color: var(--good); }
  .vote-identity-edit {
    background: transparent;
    color: var(--text-faint);
    border: none;
    font-size: 11px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    cursor: pointer;
    font-family: inherit;
    padding: 2px 4px;
  }
  .vote-identity-edit:hover { color: var(--accent); }
  .vote-tally-pill {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    font-size: 11px;
    font-weight: 700;
    color: var(--accent);
    background: rgba(245,179,66,0.10);
    border: 1px solid rgba(245,179,66,0.25);
    border-radius: 999px;
    padding: 3px 9px;
    font-variant-numeric: tabular-nums;
  }
  .vote-tally-pill svg { width: 10px; height: 10px; }
  .vote-voters {
    font-size: 11px;
    color: var(--text-faint);
    margin-top: 10px;
    padding-top: 10px;
    border-top: 1px dashed var(--line);
    line-height: 1.4;
  }
  .vote-voters:empty { display: none; }
  .vote-voters .label {
    color: var(--text-dim);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-size: 10px;
    margin-right: 4px;
  }

  /* MODAL */
  .modal-backdrop {
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.7);
    backdrop-filter: blur(6px);
    -webkit-backdrop-filter: blur(6px);
    display: none;
    align-items: center;
    justify-content: center;
    z-index: 100;
    padding: 20px;
  }
  .modal-backdrop.show { display: flex; }
  .modal {
    background: var(--bg-2);
    border: 1px solid var(--line-2);
    border-radius: 18px;
    padding: 24px 22px;
    width: 100%;
    max-width: 360px;
    box-shadow: 0 24px 60px rgba(0,0,0,0.5);
  }
  .modal-title {
    font-size: 18px;
    font-weight: 800;
    letter-spacing: -0.01em;
    margin-bottom: 4px;
  }
  .modal-sub {
    font-size: 13px;
    color: var(--text-dim);
    margin-bottom: 16px;
  }
  .modal-input {
    width: 100%;
    background: var(--bg-3);
    border: 1px solid var(--line-2);
    border-radius: 10px;
    padding: 12px 14px;
    color: var(--text);
    font-size: 14px;
    font-family: inherit;
    outline: none;
    transition: border-color 0.15s;
    margin-bottom: 12px;
    box-sizing: border-box;
  }
  .modal-input:focus { border-color: var(--accent); }
  .modal-actions {
    display: flex;
    gap: 8px;
  }
  .modal-btn {
    flex: 1;
    padding: 11px;
    border-radius: 10px;
    font-size: 13px;
    font-weight: 700;
    cursor: pointer;
    font-family: inherit;
    border: 1px solid var(--line-2);
    background: var(--bg-3);
    color: var(--text);
    transition: all 0.15s;
  }
  .modal-btn.primary {
    background: var(--accent);
    color: #1a1207;
    border-color: var(--accent);
  }
  .modal-btn:active { transform: scale(0.98); }
  .modal-error {
    color: var(--bad);
    font-size: 12px;
    margin-top: -6px;
    margin-bottom: 10px;
    min-height: 14px;
  }
  .vote-cards {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .vote-card {
    background: var(--bg-2);
    border: 1px solid var(--line);
    border-radius: 16px;
    padding: 16px;
    transition: all 0.25s;
    position: relative;
  }
  .vote-card.picked {
    border-color: var(--accent);
    box-shadow: 0 0 0 1px var(--accent), 0 8px 24px rgba(245,179,66,0.18);
    background: linear-gradient(180deg, rgba(245,179,66,0.06), var(--bg-2));
  }
  .vote-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    margin-bottom: 10px;
  }
  .vote-name {
    font-size: 15px;
    font-weight: 800;
    letter-spacing: -0.01em;
    display: flex;
    align-items: center;
    gap: 6px;
  }
  .vote-link {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    color: var(--text-faint);
    text-decoration: none;
    font-size: 11px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    background: var(--bg-3);
    padding: 5px 9px;
    border-radius: 6px;
    border: 1px solid var(--line);
    transition: all 0.15s;
  }
  .vote-link:hover { color: var(--accent); border-color: rgba(245,179,66,0.30); }
  .vote-link svg { width: 11px; height: 11px; }
  .vote-stats {
    display: flex;
    gap: 8px;
    margin-bottom: 12px;
    flex-wrap: wrap;
  }
  .vote-stat {
    font-size: 11px;
    background: var(--bg-3);
    border: 1px solid var(--line);
    border-radius: 6px;
    padding: 4px 8px;
    color: var(--text-dim);
    font-weight: 600;
    display: inline-flex;
    align-items: center;
    gap: 5px;
  }
  .vote-stat svg { width: 11px; height: 11px; color: var(--text-faint); }
  .vote-row {
    display: flex;
    align-items: flex-start;
    gap: 8px;
    font-size: 12.5px;
    line-height: 1.4;
    color: var(--text-dim);
    padding: 4px 0;
  }
  .vote-row svg {
    width: 14px; height: 14px;
    flex-shrink: 0;
    margin-top: 2px;
  }
  .vote-row.pro svg { color: var(--good); }
  .vote-row.con svg { color: var(--bad); }
  .vote-btn {
    width: 100%;
    margin-top: 12px;
    background: var(--bg-3);
    color: var(--text);
    border: 1px solid var(--line-2);
    border-radius: 10px;
    padding: 11px;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.02em;
    cursor: pointer;
    transition: all 0.2s;
    font-family: inherit;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
  }
  .vote-btn:active { transform: scale(0.98); }
  .vote-card.picked .vote-btn {
    background: var(--accent);
    color: #1a1207;
    border-color: var(--accent);
  }
  .vote-btn .picked-text { display: none; }
  .vote-card.picked .vote-btn .picked-text { display: inline; }
  .vote-card.picked .vote-btn .default-text { display: none; }
  .vote-badge {
    position: absolute;
    top: -8px;
    right: 14px;
    background: var(--accent);
    color: #1a1207;
    font-size: 9px;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    padding: 4px 8px;
    border-radius: 4px;
    display: none;
  }
  .vote-card.picked .vote-badge { display: block; }

  /* SECTION */
  .section {
    margin-top: 28px;
  }
  .section-head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    margin-bottom: 14px;
    padding: 0 4px;
  }
  .section-title {
    font-size: 15px;
    font-weight: 700;
    letter-spacing: -0.01em;
  }
  .section-meta {
    font-size: 11px;
    color: var(--text-faint);
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  /* GOALS */
  .goals {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 10px;
  }
  .goal {
    background: var(--bg-2);
    border: 1px solid var(--line);
    border-radius: 16px;
    padding: 14px 14px 12px;
    cursor: pointer;
    transition: all 0.2s;
    user-select: none;
    position: relative;
    overflow: hidden;
  }
  .goal:active { transform: scale(0.98); }
  .goal-icon {
    width: 36px; height: 36px;
    border-radius: 10px;
    display: grid;
    place-items: center;
    margin-bottom: 10px;
  }
  .goal-icon svg { width: 18px; height: 18px; }
  .goal[data-type="hike"] .goal-icon { background: rgba(62,224,138,0.12); color: var(--hike); }
  .goal[data-type="stay"] .goal-icon { background: rgba(245,179,66,0.12); color: var(--cabin); }
  .goal[data-type="bar"] .goal-icon { background: rgba(255,93,143,0.12); color: var(--bar); }
  .goal[data-type="casino"] .goal-icon { background: rgba(184,124,255,0.12); color: var(--casino); }
  .goal-title {
    font-size: 13px;
    font-weight: 700;
    margin-bottom: 2px;
  }
  .goal-sub {
    font-size: 11px;
    color: var(--text-faint);
    line-height: 1.35;
  }
  .goal-check {
    position: absolute;
    top: 12px; right: 12px;
    width: 22px; height: 22px;
    border-radius: 50%;
    border: 1.5px solid var(--line-2);
    background: var(--bg-3);
    display: grid;
    place-items: center;
    transition: all 0.2s;
  }
  .goal-check svg { width: 12px; height: 12px; color: transparent; transition: color 0.2s; }
  .goal.done .goal-check {
    background: var(--good);
    border-color: var(--good);
  }
  .goal.done .goal-check svg { color: #062810; }
  .goal.done { border-color: rgba(62,224,138,0.30); }
  .goals-progress {
    margin-top: 12px;
    height: 4px;
    background: var(--bg-2);
    border-radius: 2px;
    overflow: hidden;
  }
  .goals-progress-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--accent), var(--good));
    transition: width 0.4s ease;
  }

  /* TIMELINE */
  .timeline {
    position: relative;
    padding-left: 26px;
  }
  .timeline::before {
    content: '';
    position: absolute;
    left: 9px;
    top: 8px;
    bottom: 8px;
    width: 2px;
    background: linear-gradient(180deg, var(--line-2), var(--line));
  }
  .day-divider {
    margin: 18px 0 12px -26px;
    padding-left: 26px;
    font-size: 11px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--text-faint);
    position: relative;
  }
  .day-divider:first-child { margin-top: 0; }
  .day-divider span {
    background: var(--bg-0);
    padding-right: 8px;
  }
  .tl-item {
    position: relative;
    margin-bottom: 12px;
  }
  .tl-dot {
    position: absolute;
    left: -22px;
    top: 18px;
    width: 14px; height: 14px;
    border-radius: 50%;
    background: var(--bg-1);
    border: 2px solid var(--accent);
    z-index: 2;
  }
  .tl-item[data-type="casino"] .tl-dot { border-color: var(--casino); }
  .tl-item[data-type="hike"] .tl-dot { border-color: var(--hike); }
  .tl-item[data-type="cabin"] .tl-dot { border-color: var(--cabin); }
  .tl-item[data-type="bar"] .tl-dot { border-color: var(--bar); }
  .tl-card {
    background: var(--bg-2);
    border: 1px solid var(--line);
    border-radius: 14px;
    padding: 14px;
    cursor: pointer;
    transition: all 0.2s;
  }
  .tl-card:active { transform: scale(0.99); }
  .tl-card.expanded { border-color: var(--line-2); }
  .tl-row {
    display: flex;
    align-items: flex-start;
    gap: 12px;
  }
  .tl-icon {
    width: 36px; height: 36px;
    border-radius: 10px;
    display: grid;
    place-items: center;
    flex-shrink: 0;
  }
  .tl-icon svg { width: 18px; height: 18px; }
  .tl-item[data-type="casino"] .tl-icon { background: rgba(184,124,255,0.12); color: var(--casino); }
  .tl-item[data-type="hike"] .tl-icon { background: rgba(62,224,138,0.12); color: var(--hike); }
  .tl-item[data-type="cabin"] .tl-icon { background: rgba(245,179,66,0.12); color: var(--cabin); }
  .tl-item[data-type="bar"] .tl-icon { background: rgba(255,93,143,0.12); color: var(--bar); }
  .tl-meta { flex: 1; min-width: 0; }
  .tl-time {
    font-size: 11px;
    color: var(--text-faint);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: 2px;
  }
  .tl-title {
    font-size: 14px;
    font-weight: 700;
    letter-spacing: -0.01em;
  }
  .tl-tag {
    display: inline-block;
    font-size: 10px;
    margin-left: 6px;
    padding: 2px 6px;
    border-radius: 4px;
    background: var(--bg-3);
    color: var(--text-faint);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    vertical-align: middle;
  }
  .tl-detail {
    margin-top: 10px;
    padding-top: 10px;
    border-top: 1px dashed var(--line);
    font-size: 12.5px;
    color: var(--text-dim);
    line-height: 1.5;
    display: none;
  }
  .tl-card.expanded .tl-detail { display: block; }
  .tl-chevron {
    color: var(--text-faint);
    transition: transform 0.2s;
  }
  .tl-card.expanded .tl-chevron { transform: rotate(180deg); }

  /* PACKING */
  .pack-list {
    background: var(--bg-2);
    border: 1px solid var(--line);
    border-radius: 16px;
    padding: 6px 14px;
  }
  .pack-item {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 12px 0;
    border-bottom: 1px solid var(--line);
    cursor: pointer;
    user-select: none;
  }
  .pack-item:last-child { border-bottom: none; }
  .pack-check {
    width: 22px; height: 22px;
    border-radius: 6px;
    border: 1.5px solid var(--line-2);
    background: var(--bg-3);
    flex-shrink: 0;
    display: grid;
    place-items: center;
    transition: all 0.15s;
  }
  .pack-check svg { width: 12px; height: 12px; color: transparent; }
  .pack-item.done .pack-check {
    background: var(--accent);
    border-color: var(--accent);
  }
  .pack-item.done .pack-check svg { color: #1a1207; }
  .pack-icon {
    width: 32px; height: 32px;
    border-radius: 8px;
    background: var(--bg-3);
    display: grid;
    place-items: center;
    color: var(--text-dim);
    flex-shrink: 0;
  }
  .pack-icon svg { width: 16px; height: 16px; }
  .pack-text {
    flex: 1;
    font-size: 13.5px;
    font-weight: 500;
    transition: all 0.15s;
  }
  .pack-text-sub {
    font-size: 11px;
    color: var(--text-faint);
    margin-top: 2px;
    font-weight: 400;
  }
  .pack-item.done .pack-text { color: var(--text-faint); text-decoration: line-through; }

  /* FOOTER */
  .footer {
    margin-top: 36px;
    text-align: center;
    color: var(--text-faint);
    font-size: 11px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }
  .footer span { color: var(--accent); }
</style>
</head>
<body>
<div class="app">

  <!-- HEADER -->
  <div class="header">
    <div class="brand">
      <div class="brand-mark">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M6 3h12l4 6-10 13L2 9z"/><path d="M11 3 8 9l4 13 4-13-3-6"/><path d="M2 9h20"/></svg>
      </div>
      <div>
        <div class="brand-name">Brandon's Send-Off</div>
        <div class="brand-sub">Charles Town · Jun 5–7</div>
      </div>
    </div>
    <div class="live-dot">LIVE</div>
  </div>

  <!-- HERO / COUNTDOWN -->
  <div class="hero-card">
    <span class="hero-tag">★ MAIN EVENT</span>
    <div class="hero-title">The Send-Off Weekend</div>
    <div class="hero-sub">Cabin · Hike · Casino · Campfire</div>

    <div class="countdown-label">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
      Cabin check-in
    </div>
    <div class="countdown" id="countdown">
      <div class="cd-cell"><div class="cd-num" id="cd-d">--</div><div class="cd-lbl">Days</div></div>
      <div class="cd-cell"><div class="cd-num" id="cd-h">--</div><div class="cd-lbl">Hrs</div></div>
      <div class="cd-cell"><div class="cd-num" id="cd-m">--</div><div class="cd-lbl">Min</div></div>
      <div class="cd-cell"><div class="cd-num" id="cd-s">--</div><div class="cd-lbl">Sec</div></div>
    </div>
  </div>

  <!-- CABIN VOTE -->
  <div class="section">
    <div class="section-head">
      <div class="section-title">Cabin Vote</div>
      <div class="section-meta" id="vote-status">No pick yet</div>
    </div>
    <div class="vote-banner" id="vote-banner">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 11l3 3L22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>
      <div id="vote-banner-text">Tap your favorite — votes sync live to the group sheet.</div>
    </div>
    <div class="vote-identity" id="vote-identity" style="display:none">
      <div class="vote-identity-name">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
        <span>Voting as</span>
        <strong id="voter-name-display">—</strong>
      </div>
      <button class="vote-identity-edit" id="change-name-btn">Change</button>
    </div>
    <div class="vote-cards" id="votecards">

      <div class="vote-card" data-id="at">
        <div class="vote-badge">Your Pick</div>
        <div class="vote-head">
          <div class="vote-name">The A.T. Cabin</div>
          <div style="display:flex;align-items:center;gap:6px">
            <span class="vote-tally-pill" data-tally="at">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/></svg>
              <span data-tally-num="at">0</span>
            </span>
            <a class="vote-link" href="https://www.airbnb.com/rooms/624777244529903055" target="_blank" rel="noopener" onclick="event.stopPropagation()">
              View
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/></svg>
            </a>
          </div>
        </div>
        <div class="vote-stats">
          <span class="vote-stat"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>Sleeps 6</span>
          <span class="vote-stat"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 4v16"/><path d="M22 4v16"/><path d="M2 8h20"/><path d="M2 14h20"/></svg>4 beds</span>
          <span class="vote-stat"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>15m to casino</span>
        </div>
        <div class="vote-row pro"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>Very secluded — true off-the-grid feel</div>
        <div class="vote-row pro"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>Closest to Hollywood Casino (15 min)</div>
        <div class="vote-row con"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/></svg>Strict 10 PM quiet hours + noise monitors</div>
        <button class="vote-btn"><span class="default-text">Vote for A.T. Cabin</span><span class="picked-text">✓ Your Pick</span></button>
        <div class="vote-voters" data-voters="at"></div>
      </div>

      <div class="vote-card" data-id="log">
        <div class="vote-badge">Your Pick</div>
        <div class="vote-head">
          <div class="vote-name">The Log Cabin</div>
          <div style="display:flex;align-items:center;gap:6px">
            <span class="vote-tally-pill" data-tally="log">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/></svg>
              <span data-tally-num="log">0</span>
            </span>
            <a class="vote-link" href="https://www.airbnb.com/rooms/1452371397472114695" target="_blank" rel="noopener" onclick="event.stopPropagation()">
              View
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/></svg>
            </a>
          </div>
        </div>
        <div class="vote-stats">
          <span class="vote-stat"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>Sleeps 6</span>
          <span class="vote-stat"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 4v16"/><path d="M22 4v16"/><path d="M2 8h20"/><path d="M2 14h20"/></svg>3 beds</span>
          <span class="vote-stat"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M8.5 14.5A2.5 2.5 0 0 0 11 12c0-1.38-.5-2-1-3-1.072-2.143-.224-4.054 2-6 .5 2.5 2 4.9 4 6.5 2 1.6 3 3.5 3 5.5a7 7 0 1 1-14 0c0-1.153.433-2.294 1-3a2.5 2.5 0 0 0 2.5 2.5"/></svg>Wood fireplace</span>
        </div>
        <div class="vote-row pro"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>Classic mountain lodge feel</div>
        <div class="vote-row pro"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>Real wood fireplace + fenced yard</div>
        <div class="vote-row con"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/></svg>Only 3 actual beds (some sharing)</div>
        <button class="vote-btn"><span class="default-text">Vote for Log Cabin</span><span class="picked-text">✓ Your Pick</span></button>
        <div class="vote-voters" data-voters="log"></div>
      </div>

      <div class="vote-card" data-id="huck">
        <div class="vote-badge">Your Pick</div>
        <div class="vote-head">
          <div class="vote-name">Huckleberry Hills</div>
          <div style="display:flex;align-items:center;gap:6px">
            <span class="vote-tally-pill" data-tally="huck">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/></svg>
              <span data-tally-num="huck">0</span>
            </span>
            <a class="vote-link" href="https://www.airbnb.com/rooms/45733761" target="_blank" rel="noopener" onclick="event.stopPropagation()">
              View
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/></svg>
            </a>
          </div>
        </div>
        <div class="vote-stats">
          <span class="vote-stat"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>Sleeps 6</span>
          <span class="vote-stat"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 4v16"/><path d="M22 4v16"/><path d="M2 8h20"/><path d="M2 14h20"/></svg>6 beds</span>
          <span class="vote-stat"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 12h20"/><path d="M5 12V8a3 3 0 0 1 6 0v4"/><path d="M13 12V8a3 3 0 0 1 6 0v4"/><path d="M2 16c2 0 2 2 4 2s2-2 4-2 2 2 4 2 2-2 4-2 2 2 4 2"/></svg>Hot tub</span>
        </div>
        <div class="vote-row pro"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>Hot tub + fire pit + game room with ping pong</div>
        <div class="vote-row pro"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>6 actual beds — everyone has their own</div>
        <div class="vote-row con"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/></svg>Furthest drive — 20–25 min to town</div>
        <button class="vote-btn"><span class="default-text">Vote for Huckleberry Hills</span><span class="picked-text">✓ Your Pick</span></button>
        <div class="vote-voters" data-voters="huck"></div>
      </div>

    </div>
  </div>

  <!-- 4 GOALS -->
  <div class="section">
    <div class="section-head">
      <div class="section-title">The 4 Goals</div>
      <div class="section-meta" id="goals-count">0 / 4</div>
    </div>
    <div class="goals" id="goals">
      <div class="goal" data-type="hike" data-id="hike">
        <div class="goal-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m8 21 4-7 4 7"/><path d="m2 21 6-12 4 6 4-9 6 15"/></svg>
        </div>
        <div class="goal-title">Hike</div>
        <div class="goal-sub">Maryland Heights · 4.5mi</div>
        <div class="goal-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></div>
      </div>
      <div class="goal" data-type="stay" data-id="stay">
        <div class="goal-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
        </div>
        <div class="goal-title">Stay</div>
        <div class="goal-sub">Secluded Cabin · 2 nights</div>
        <div class="goal-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></div>
      </div>
      <div class="goal" data-type="bar" data-id="bar">
        <div class="goal-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M8 22h8"/><path d="M7 10h10l-3 5h-4z"/><path d="M12 15v7"/><path d="M5 2h14l-2 8H7z"/></svg>
        </div>
        <div class="goal-title">Bar Hop</div>
        <div class="goal-sub">Downtown Charles Town</div>
        <div class="goal-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></div>
      </div>
      <div class="goal" data-type="casino" data-id="casino">
        <div class="goal-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="3"/><circle cx="8" cy="8" r="1.2" fill="currentColor"/><circle cx="16" cy="16" r="1.2" fill="currentColor"/><circle cx="12" cy="12" r="1.2" fill="currentColor"/><circle cx="16" cy="8" r="1.2" fill="currentColor"/><circle cx="8" cy="16" r="1.2" fill="currentColor"/></svg>
        </div>
        <div class="goal-title">Casino</div>
        <div class="goal-sub">Hollywood · Tables & Slots</div>
        <div class="goal-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></div>
      </div>
    </div>
    <div class="goals-progress"><div class="goals-progress-fill" id="goals-bar" style="width: 0%"></div></div>
  </div>

  <!-- TIMELINE -->
  <div class="section">
    <div class="section-head">
      <div class="section-title">Itinerary</div>
      <div class="section-meta">Tap to expand</div>
    </div>
    <div class="timeline" id="timeline"></div>
  </div>

  <!-- PACKING -->
  <div class="section">
    <div class="section-head">
      <div class="section-title">Packing List</div>
      <div class="section-meta" id="pack-count">0 / 0</div>
    </div>
    <div class="pack-list" id="packlist"></div>
  </div>

  <div class="footer">Best Man Mode · <span>Brandon</span> · Locked In</div>
</div>

<!-- NAME MODAL -->
<div class="modal-backdrop" id="name-modal">
  <div class="modal">
    <div class="modal-title">Who's voting?</div>
    <div class="modal-sub">Quick — drop your name so the group can see who picked what.</div>
    <input type="text" class="modal-input" id="name-input" placeholder="Your first name" autocomplete="given-name" maxlength="40" />
    <div class="modal-error" id="name-error"></div>
    <div class="modal-actions">
      <button class="modal-btn" id="name-cancel">Cancel</button>
      <button class="modal-btn primary" id="name-save">Save</button>
    </div>
  </div>
</div>

<script>
  // =====================================================================
  // CONFIG — paste your Apps Script Web App URL between the quotes:
  // =====================================================================
  const WEB_APP_URL = 'https://script.google.com/macros/s/AKfycbwwxrxiAu4ruh6JS3l5qDWvC7jwc71V8n_NXS65Qn0iFcvBXBNhlkU9RQncrNHc8J2IwA/exec';
  // =====================================================================

  // ===== COUNTDOWN =====
  // Friday, June 5, 2026 3:00 PM (local time)
  const target = new Date(2026, 5, 5, 15, 0, 0).getTime();
  const cdD = document.getElementById('cd-d');
  const cdH = document.getElementById('cd-h');
  const cdM = document.getElementById('cd-m');
  const cdS = document.getElementById('cd-s');
  function pad(n){ return String(Math.max(0,n)).padStart(2,'0'); }
  function tick(){
    const now = Date.now();
    let diff = Math.max(0, target - now);
    const days = Math.floor(diff / 86400000); diff -= days*86400000;
    const hrs  = Math.floor(diff / 3600000);  diff -= hrs*3600000;
    const mins = Math.floor(diff / 60000);    diff -= mins*60000;
    const secs = Math.floor(diff / 1000);
    cdD.textContent = pad(days);
    cdH.textContent = pad(hrs);
    cdM.textContent = pad(mins);
    cdS.textContent = pad(secs);
  }
  tick(); setInterval(tick, 1000);

  // ===== STORAGE =====
  const state = {
    goals: { hike:false, stay:false, bar:false, casino:false },
    pack: {},
    timeline: {},
    vote: null,
    voterName: null
  };

  // ===== CABIN VOTE (live-synced to Google Sheet) =====
  const voteEl = document.getElementById('votecards');
  const voteStatus = document.getElementById('vote-status');
  const voteBanner = document.getElementById('vote-banner');
  const voteBannerText = document.getElementById('vote-banner-text');
  const voteIdentity = document.getElementById('vote-identity');
  const voterNameDisplay = document.getElementById('voter-name-display');
  const changeNameBtn = document.getElementById('change-name-btn');
  const voteNames = { at: 'A.T. Cabin', log: 'Log Cabin', huck: 'Huckleberry Hills' };

  const NAME_KEY = 'bachelor-voter-name';
  const isConfigured = WEB_APP_URL && !WEB_APP_URL.startsWith('__PASTE');

  // Load saved name if any
  try { state.voterName = localStorage.getItem(NAME_KEY) || null; } catch(_) {}

  function renderIdentity() {
    if (!isConfigured) {
      voteBanner.classList.add('setup');
      voteBannerText.innerHTML = '<b>Setup needed:</b> paste your Apps Script Web App URL into the HTML (top of &lt;script&gt;).';
      voteIdentity.style.display = 'none';
      return;
    }
    voteBanner.classList.remove('setup');
    if (state.voterName) {
      voterNameDisplay.textContent = state.voterName;
      voteIdentity.style.display = 'flex';
      voteBannerText.textContent = 'Tap a cabin to cast your vote — the group sees it instantly.';
    } else {
      voteIdentity.style.display = 'none';
      voteBannerText.textContent = 'Tap a cabin to vote — we\'ll ask your name first.';
    }
  }

  function renderVoteCards(tallies, voters) {
    tallies = tallies || { at:0, log:0, huck:0 };
    voters = voters || { at:[], log:[], huck:[] };
    let total = 0;
    Object.keys(tallies).forEach(id => {
      total += tallies[id];
      const numEl = document.querySelector('[data-tally-num="' + id + '"]');
      if (numEl) numEl.textContent = tallies[id];
      const votersEl = document.querySelector('[data-voters="' + id + '"]');
      if (votersEl) {
        if (voters[id].length) {
          votersEl.innerHTML = '<span class="label">Voted:</span>' + voters[id].join(', ');
        } else {
          votersEl.innerHTML = '';
        }
      }
    });
    voteEl.querySelectorAll('.vote-card').forEach(c => {
      if (c.dataset.id === state.vote) c.classList.add('picked');
      else c.classList.remove('picked');
    });
    if (state.vote) {
      voteStatus.textContent = 'You picked: ' + voteNames[state.vote] + ' · ' + total + ' total';
    } else {
      voteStatus.textContent = total ? (total + ' vote' + (total===1?'':'s') + ' so far') : 'No votes yet';
    }
  }

  // Find current user's vote in fetched tallies (case-insensitive name match)
  function reconcileMyVote(voters) {
    if (!state.voterName) { state.vote = null; return; }
    const me = state.voterName.toLowerCase();
    state.vote = null;
    Object.keys(voters || {}).forEach(id => {
      (voters[id] || []).forEach(n => {
        if (String(n).toLowerCase() === me) state.vote = id;
      });
    });
  }

  async function fetchTallies() {
    if (!isConfigured) return;
    try {
      const r = await fetch(WEB_APP_URL, { method: 'GET' });
      const data = await r.json();
      if (data && data.ok) {
        reconcileMyVote(data.voters);
        renderVoteCards(data.tallies, data.voters);
      }
    } catch (err) {
      console.warn('Tally fetch failed:', err);
    }
  }

  async function submitVote(voteId) {
    if (!isConfigured || !state.voterName) return;
    // Optimistic UI
    state.vote = voteId;
    voteEl.querySelectorAll('.vote-card').forEach(c => {
      c.classList.toggle('picked', c.dataset.id === voteId);
    });
    try {
      const r = await fetch(WEB_APP_URL, {
        method: 'POST',
        // text/plain avoids the CORS preflight that Apps Script can't handle
        headers: { 'Content-Type': 'text/plain;charset=utf-8' },
        body: JSON.stringify({ name: state.voterName, vote: voteId || '' })
      });
      const data = await r.json();
      if (data && data.ok) {
        reconcileMyVote(data.voters);
        renderVoteCards(data.tallies, data.voters);
      } else {
        console.warn('Vote rejected:', data && data.error);
        fetchTallies();
      }
    } catch (err) {
      console.warn('Vote submit failed:', err);
      fetchTallies();
    }
  }

  // ===== NAME MODAL =====
  const modal = document.getElementById('name-modal');
  const nameInput = document.getElementById('name-input');
  const nameError = document.getElementById('name-error');
  let pendingVote = null;

  function openNameModal(forVoteId) {
    pendingVote = forVoteId || null;
    nameInput.value = state.voterName || '';
    nameError.textContent = '';
    modal.classList.add('show');
    setTimeout(() => nameInput.focus(), 50);
  }
  function closeNameModal() {
    modal.classList.remove('show');
    pendingVote = null;
  }
  document.getElementById('name-cancel').addEventListener('click', closeNameModal);
  document.getElementById('name-save').addEventListener('click', () => {
    const v = (nameInput.value || '').trim();
    if (!v) { nameError.textContent = 'Need a name to vote.'; return; }
    if (v.length > 40) { nameError.textContent = 'Keep it under 40 characters.'; return; }
    state.voterName = v;
    try { localStorage.setItem(NAME_KEY, v); } catch(_) {}
    closeNameModal();
    renderIdentity();
    if (pendingVote) submitVote(pendingVote);
    else fetchTallies();
  });
  nameInput.addEventListener('keydown', e => {
    if (e.key === 'Enter') document.getElementById('name-save').click();
  });
  modal.addEventListener('click', e => {
    if (e.target === modal) closeNameModal();
  });
  changeNameBtn.addEventListener('click', () => openNameModal(null));

  // Card click → vote (or open name modal first)
  voteEl.addEventListener('click', e => {
    if (e.target.closest('a')) return;
    const card = e.target.closest('.vote-card');
    if (!card) return;
    if (!isConfigured) return;
    const voteId = card.dataset.id;
    // Toggle off if clicking your existing pick
    const newVote = (state.vote === voteId) ? '' : voteId;
    if (!state.voterName) {
      openNameModal(newVote || voteId);
      return;
    }
    submitVote(newVote);
  });

  // Initial render + periodic refresh
  renderIdentity();
  renderVoteCards();
  fetchTallies();
  setInterval(fetchTallies, 20000);

  // ===== GOALS =====
  const goalsEl = document.getElementById('goals');
  const goalsCountEl = document.getElementById('goals-count');
  const goalsBar = document.getElementById('goals-bar');
  function renderGoals() {
    let done = 0;
    goalsEl.querySelectorAll('.goal').forEach(g => {
      const id = g.dataset.id;
      if (state.goals[id]) { g.classList.add('done'); done++; }
      else g.classList.remove('done');
    });
    goalsCountEl.textContent = done + ' / 4';
    goalsBar.style.width = (done/4*100) + '%';
  }
  goalsEl.addEventListener('click', e => {
    const g = e.target.closest('.goal');
    if (!g) return;
    state.goals[g.dataset.id] = !state.goals[g.dataset.id];
    renderGoals();
  });
  renderGoals();

  // ===== TIMELINE =====
  const tlData = [
    { day: 'Friday · June 5', items: [
      { id:'arrive', time:'3:00 PM', title:'Check-in & Basecamp', type:'cabin', tag:'Arrival',
        icon:'<path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/>',
        detail:"Get to the cabin, drinks in the fridge, claim beds. The first hour sets the tone — keep it loose." },
      { id:'social', time:'4:00 PM', title:'Cabin Social Hour', type:'cabin', tag:'Hangout',
        icon:'<path d="M8 22h8"/><path d="M7 10h10l-3 5h-4z"/><path d="M12 15v7"/><path d="M5 2h14l-2 8H7z"/>',
        detail:'Board games and a few drinks while everyone arrives. No pressure — just settle in.' },
      { id:'casinoF', time:'7:30 PM', title:'Casino Night', type:'casino', tag:'Main',
        icon:'<rect x="3" y="3" width="18" height="18" rx="3"/><circle cx="8" cy="8" r="1.2" fill="currentColor"/><circle cx="16" cy="16" r="1.2" fill="currentColor"/><circle cx="12" cy="12" r="1.2" fill="currentColor"/><circle cx="16" cy="8" r="1.2" fill="currentColor"/><circle cx="8" cy="16" r="1.2" fill="currentColor"/>',
        detail:'Hollywood Casino. Dinner at the sportsbook or steakhouse first, then tables and slots. Set a budget before you walk in.' }
    ]},
    { day: 'Saturday · June 6', items: [
      { id:'fuel', time:'9:30 AM', title:'Breakfast / Fuel', type:'cabin', tag:'Quick',
        icon:'<path d="M6 13.87A4 4 0 0 1 7.41 6a5.11 5.11 0 0 1 1.05-1.54 5 5 0 0 1 7.08 0A5.11 5.11 0 0 1 16.59 6 4 4 0 0 1 18 13.87V21H6Z"/><line x1="6" y1="17" x2="18" y2="17"/>',
        detail:'Eggs, coffee, sandwiches — whatever gets the group out the door. Carpool to the trailhead.' },
      { id:'hike', time:'10:00 AM', title:'Maryland Heights Hike', type:'hike', tag:'4.5 mi',
        icon:'<path d="m8 21 4-7 4 7"/><path d="m2 21 6-12 4 6 4-9 6 15"/>',
        detail:"The big push — 4.5 miles, panoramic views over Harpers Ferry. We need to be at the trailhead early to grab a parking spot." },
      { id:'lunch', time:'1:30 PM', title:'Post-Hike Lunch', type:'bar', tag:'Heavy',
        icon:'<path d="M3 7h18l-2 13H5z"/><path d="M8 7V5a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/>',
        detail:'The Rabbit Hole or Harpers Ferry Brewing. Order big — we earned it.' },
      { id:'lockdown', time:'3:30 PM', title:'Cabin Lockdown', type:'cabin', tag:'Recovery',
        icon:'<path d="M12 2 4 7v6c0 5 3.5 8.5 8 9 4.5-.5 8-4 8-9V7z"/>',
        detail:'Hot tub, intense board games, general recovery. The unscheduled block is sacred — protect it.' },
      { id:'campfire', time:'7:30 PM', title:'The Main Campfire', type:'cabin', tag:'★ Toasts',
        icon:'<path d="M8.5 14.5A2.5 2.5 0 0 0 11 12c0-1.38-.5-2-1-3-1.072-2.143-.224-4.054 2-6 .5 2.5 2 4.9 4 6.5 2 1.6 3 3.5 3 5.5a7 7 0 1 1-14 0c0-1.153.433-2.294 1-3a2.5 2.5 0 0 0 2.5 2.5"/>',
        detail:"Fire pit, grilling, and the bachelor party toasts. The cabin's fire pit and wood are covered." },
      { id:'townrun', time:'10:00 PM', title:'Optional Town Run', type:'bar', tag:'Optional',
        icon:'<path d="M8 22h8"/><path d="M7 10h10l-3 5h-4z"/><path d="M12 15v7"/><path d="M5 2h14l-2 8H7z"/>',
        detail:'Uber into downtown Charles Town for a final round. Skip if the fire is still going strong.' }
    ]},
    { day: 'Sunday · June 7', items: [
      { id:'checkout', time:'11:00 AM', title:'Check-out', type:'cabin', tag:'Hard stop',
        icon:'<path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/>',
        detail:'Final sweep, trash out, all lights off. Hit the road.' }
    ]}
  ];
  const tlEl = document.getElementById('timeline');
  let tlHTML = '';
  tlData.forEach(group => {
    tlHTML += '<div class="day-divider"><span>' + group.day + '</span></div>';
    group.items.forEach(it => {
      tlHTML += `
        <div class="tl-item" data-type="${it.type}">
          <div class="tl-dot"></div>
          <div class="tl-card" data-id="${it.id}">
            <div class="tl-row">
              <div class="tl-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">${it.icon}</svg>
              </div>
              <div class="tl-meta">
                <div class="tl-time">${it.time}</div>
                <div class="tl-title">${it.title}<span class="tl-tag">${it.tag}</span></div>
              </div>
              <div class="tl-chevron">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
              </div>
            </div>
            <div class="tl-detail">${it.detail}</div>
          </div>
        </div>`;
    });
  });
  tlEl.innerHTML = tlHTML;
  tlEl.addEventListener('click', e => {
    const card = e.target.closest('.tl-card');
    if (!card) return;
    card.classList.toggle('expanded');
  });

  // ===== PACKING LIST =====
  const packData = [
    { id:'boards',  text:'Board games',                    sub:'Codenames, Catan, Cards Against Humanity', icon:'<rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M3 15h18M9 3v18M15 3v18"/>' },
    { id:'boots',   text:'Hiking boots',                   sub:'Maryland Heights is rocky',                   icon:'<path d="M4 14V6a2 2 0 0 1 2-2h6v10"/><path d="M2 14h18a2 2 0 0 1 2 2v2H2z"/><path d="M14 8l4 2"/>' },
    { id:'casino',  text:'"Nice" clothes for the casino',  sub:'Collared shirt minimum',                       icon:'<path d="M20.38 3.46 16 2a4 4 0 0 1-8 0L3.62 3.46a2 2 0 0 0-1.34 2.23l.58 3.47a1 1 0 0 0 .99.84H6v10c0 1.1.9 2 2 2h8a2 2 0 0 0 2-2V10h2.15a1 1 0 0 0 .99-.84l.58-3.47a2 2 0 0 0-1.34-2.23z"/>' },
    { id:'swim',    text:'Swimsuit for the hot tub',       sub:'Saturday afternoon recovery',                  icon:'<path d="M2 12h20"/><path d="M5 12V8a3 3 0 0 1 6 0v4"/><path d="M13 12V8a3 3 0 0 1 6 0v4"/><path d="M2 16c2 0 2 2 4 2s2-2 4-2 2 2 4 2 2-2 4-2 2 2 4 2"/>' },
    { id:'cash',    text:'Cash for the casino',            sub:'ATMs at Hollywood charge fees',                icon:'<rect x="2" y="6" width="20" height="12" rx="2"/><circle cx="12" cy="12" r="2"/><path d="M6 12h.01M18 12h.01"/>' },
    { id:'water',   text:'Water bottles & snacks',         sub:'For the trail',                                icon:'<path d="M8 2h8v3l-1 1v15a2 2 0 0 1-2 2h-2a2 2 0 0 1-2-2V6L8 5z"/>' },
    { id:'sun',     text:'Sunscreen & sunglasses',         sub:"It's the ridge — no shade",                    icon:'<circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M4.93 19.07l1.41-1.41M17.66 6.34l1.41-1.41"/>' },
    { id:'toast',   text:'Toast notes / speech',           sub:"For Saturday night fire",                      icon:'<path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="9" y1="15" x2="15" y2="15"/>' }
  ];
  const packEl = document.getElementById('packlist');
  const packCount = document.getElementById('pack-count');
  packEl.innerHTML = packData.map(p => `
    <div class="pack-item" data-id="${p.id}">
      <div class="pack-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></div>
      <div class="pack-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">${p.icon}</svg></div>
      <div class="pack-text">${p.text}<div class="pack-text-sub">${p.sub}</div></div>
    </div>
  `).join('');
  function renderPack() {
    let done = 0;
    packEl.querySelectorAll('.pack-item').forEach(it => {
      const id = it.dataset.id;
      if (state.pack[id]) { it.classList.add('done'); done++; }
      else it.classList.remove('done');
    });
    packCount.textContent = done + ' / ' + packData.length;
  }
  packEl.addEventListener('click', e => {
    const it = e.target.closest('.pack-item');
    if (!it) return;
    state.pack[it.dataset.id] = !state.pack[it.dataset.id];
    renderPack();
  });
  renderPack();
</script>
</body>
</html>
